# 🔗 سيناريو اختبار الترابط بين الشاشات

## 🎯 الهدف: التأكد من أن جميع الشاشات مترابطة ومتزامنة

### 📋 **سيناريو الاختبار الشامل:**

## 1. 🏠 **Dashboard (الشاشة الرئيسية)**

### ✅ **البيانات المعروضة:**
- Sprint Health Score: 57/100
- Velocity: 20 pts
- Completion: 32%
- Blockers: 1
- Team: 8 أعضاء
- Issues: 14 مهمة

### 🔗 **الترابط مع الشاشات الأخرى:**
- عند الضغط على "Ask Rovo AI" → ينقل لـ AI Chat
- عند الضغط على Team metrics → ينقل لـ Team Management
- عند الضغط على Recommendations → ينقل لـ Apply Recommendations

---

## 2. ⚙️ **Project Setup**

### 📝 **ما يجب أن يحدث:**
1. المستخدم يدخل بيانات Jira:
   - URL: https://samarabdelhamed77.atlassian.net
   - Username: samarabdelhamed77@gmail.com
   - API Token: [موجود]
   - Project Key: BTS

2. عند الضغط على "Test Connection":
   - يختبر الاتصال مع Jira
   - يعرض رسالة نجاح/فشل

3. عند الضغط على "Save Configuration":
   - يحفظ البيانات في قاعدة البيانات
   - يحدث Dashboard تلقائياً

### 🔄 **التأثير على الشاشات الأخرى:**
- Dashboard يعرض بيانات Jira الحقيقية
- Team Management يعرض أعضاء الفريق من Jira
- Sprint Goals يعرض أهداف السبرينت

---

## 3. 🎯 **Sprint Goals**

### 📝 **ما يجب أن يحدث:**
1. المستخدم يضيف هدف جديد:
   - Title: "Complete Payment Integration"
   - Description: "Finish Stripe integration"
   - Priority: High
   - Target Date: 2025-01-01

2. عند الضغط على "Add Goal":
   - يحفظ الهدف في قاعدة البيانات
   - يظهر في قائمة الأهداف فوراً

### 🔄 **التأثير على الشاشات الأخرى:**
- Dashboard يعرض تقدم الأهداف
- AI Chat يعرف عن الأهداف الجديدة
- Recommendations تأخذ الأهداف في الاعتبار

---

## 4. 👥 **Team Management**

### 📝 **ما يجب أن يحدث:**
1. المستخدم يحدث معلومات عضو:
   - Name: Sarah Johnson
   - Capacity: 25 hours/week
   - Skills: React, Node.js

2. عند الضغط على "Update":
   - يحفظ التحديثات في قاعدة البيانات
   - يعيد حساب Team Load

### 🔄 **التأثير على الشاشات الأخرى:**
- Dashboard يعرض Team Load المحدث
- Recommendations تأخذ Capacity الجديد في الاعتبار
- AI Chat يعرف عن تحديثات الفريق

---

## 5. 🔔 **Alert Settings**

### 📝 **ما يجب أن يحدث:**
1. المستخدم يضبط تنبيه:
   - Health Score < 60%
   - Notification: Email + Slack
   - Recipients: team@company.com

2. عند الضغط على "Save Settings":
   - يحفظ الإعدادات
   - يفعل نظام التنبيهات

### 🔄 **التأثير على النظام:**
- عندما Health Score ينخفض < 60%
- يرسل تنبيه تلقائياً
- Dashboard يعرض Alert Status

---

## 6. 🤖 **AI Chat**

### 📝 **ما يجب أن يحدث:**
1. المستخدم يسأل: "كيف أداء الفريق؟"
2. AI يجيب بناءً على البيانات الحقيقية:
   - Sprint Health: 57%
   - Team Load: Sarah 72%, Mike 35%
   - Blockers: 1 مهمة محجوبة

### 🔄 **الترابط مع البيانات:**
- يقرأ من نفس قاعدة البيانات
- يعرض معلومات محدثة فوراً
- يقترح حلول بناءً على الوضع الحالي

---

## 7. ⚡ **Apply Recommendations**

### 📝 **ما يجب أن يحدث:**
1. النظام يقترح توصيات:
   - "إعادة توزيع المهام على الفريق"
   - "تقليل نطاق السبرينت"

2. عند الضغط على "Apply":
   - ينفذ التوصية
   - يحدث البيانات في قاعدة البيانات

### 🔄 **التأثير على الشاشات الأخرى:**
- Dashboard يعرض التحسن في Health Score
- Team Management يعرض التوزيع الجديد
- AI Chat يعرف عن التغييرات المطبقة

---

## 🧪 **اختبار الترابط الفعلي:**

### المرحلة 1: اختبار تدفق البيانات
```bash
# 1. تحقق من البيانات الأساسية
curl http://localhost:3001/api/sprint/current

# 2. اختبار AI Chat
curl -X POST http://localhost:3001/api/ai-chat \
  -H "Content-Type: application/json" \
  -d '{"message":"كيف أداء الفريق؟"}'

# 3. اختبار Recommendations
curl http://localhost:3001/api/recommendations
```

### المرحلة 2: اختبار التحديثات
```bash
# 1. إضافة هدف جديد
curl -X POST http://localhost:3001/api/sprint-goals \
  -H "Content-Type: application/json" \
  -d '{"title":"Test Goal","priority":"high"}'

# 2. تحقق من ظهوره في Dashboard
curl http://localhost:3001/api/sprint/current

# 3. اسأل AI عن الهدف الجديد
curl -X POST http://localhost:3001/api/ai-chat \
  -H "Content-Type: application/json" \
  -d '{"message":"ما هي أهداف السبرينت؟"}'
```

### المرحلة 3: اختبار التفاعل
```bash
# 1. تطبيق توصية
curl -X POST http://localhost:3001/api/recommendations/apply \
  -H "Content-Type: application/json" \
  -d '{"recommendationId":1,"action":"redistribute_tasks"}'

# 2. تحقق من التأثير على Health Score
curl http://localhost:3001/api/sprint/current

# 3. اسأل AI عن التحسن
curl -X POST http://localhost:3001/api/ai-chat \
  -H "Content-Type: application/json" \
  -d '{"message":"هل تحسن أداء السبرينت؟"}'
```

## ✅ **النتائج المتوقعة:**

### إذا كان الترابط يعمل بشكل صحيح:
1. **البيانات متزامنة**: أي تغيير في شاشة يظهر في الأخرى فوراً
2. **AI محدث**: يعرف عن جميع التغييرات الحديثة
3. **Dashboard ديناميكي**: يعكس الحالة الحقيقية للمشروع
4. **Recommendations ذكية**: تتكيف مع التغييرات
5. **Alerts فعالة**: تعمل بناءً على البيانات الحقيقية

### إذا كان هناك مشاكل:
1. **بيانات قديمة**: الشاشات لا تتحدث مع بعض
2. **AI غير محدث**: يعطي معلومات خاطئة
3. **Dashboard ثابت**: لا يتغير مع الإدخالات
4. **Recommendations عامة**: لا تعكس الوضع الحالي

## 🔧 **خطة الإصلاح:**

إذا وجدنا مشاكل، سنقوم بـ:
1. إصلاح API endpoints المفقودة
2. تحديث Context providers في React
3. إضافة real-time updates
4. تحسين database queries
5. إضافة error handling أفضل